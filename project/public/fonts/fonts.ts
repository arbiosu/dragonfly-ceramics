import { Manrope, Inter } from 'next/font/google';

export const manrope = Manrope({
  subsets: ['latin'],
  display: 'swap',
});

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});
