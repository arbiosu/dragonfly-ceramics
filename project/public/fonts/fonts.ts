import { Raleway, Inter } from 'next/font/google';

export const ralewayLight = Raleway({
  subsets: ['latin'],
  display: 'swap',
});

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});
